"""SCHEDULING tables (Member A): busy_blocks, scheduling_requests,
slot_proposals, events, event_attendees.

Owner writes the ORM mappings here, mirroring schema.sql. Left empty on purpose —
this file is imported by app/models/__init__ so Base.metadata sees it once populated.
"""
